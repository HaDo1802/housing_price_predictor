# Complete MLOps Workflow - Visual Guide

## 📊 THE COMPLETE DATA FLOW

```
┌─────────────────────────────────────────────────────────────────┐
│                        RAW DATA                                  │
│                    (data/raw/data.csv)                          │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│              STEP 1: DATA LOADING                                │
│                                                                  │
│  - Load from various formats (CSV, ZIP, Parquet)                │
│  - Initial data validation                                       │
│                                                                  │
│  Module: DataLoader or TrainingPipeline.load_data()            │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│              STEP 2: DATA CLEANING                               │
│                   (NO LEARNING!)                                 │
│                                                                  │
│  Operations that DON'T learn from data:                         │
│  ✓ Remove duplicates                                            │
│  ✓ Fix data types                                               │
│  ✓ Basic validation                                             │
│  ✗ NO imputation (that learns statistics!)                      │
│  ✗ NO outlier removal (that uses statistics!)                   │
│                                                                  │
│  Module: DataCleaner                                            │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│         ⚠️  CRITICAL STEP: SPLIT DATA  ⚠️                       │
│                                                                  │
│              Raw Cleaned Data (100%)                            │
│                      │                                           │
│         ┌────────────┼────────────┐                            │
│         │            │            │                             │
│         ▼            ▼            ▼                             │
│    TRAIN (70%)   VAL (10%)   TEST (20%)                        │
│                                                                  │
│  WHY HERE?                                                      │
│  - After cleaning (no learning)                                 │
│  - Before preprocessing (has learning)                          │
│  - PREVENTS DATA LEAKAGE!                                       │
│                                                                  │
│  Module: DataSplitter                                           │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │
      ┌──────────────────┼──────────────────┐
      │                  │                  │
      ▼                  ▼                  ▼
┌──────────┐      ┌──────────┐      ┌──────────┐
│  TRAIN   │      │   VAL    │      │   TEST   │
│  SET     │      │   SET    │      │   SET    │
└─────┬────┘      └────┬─────┘      └────┬─────┘
      │                │                  │
      │                │                  │
      ▼                │                  │
┌─────────────────────────────────────────────────────────────────┐
│         STEP 3: PREPROCESSING (LEARNING!)                        │
│                                                                  │
│  FIT on TRAIN set ONLY:                                         │
│  ├─ Calculate mean/median (for imputation)                      │
│  ├─ Calculate outlier bounds                                    │
│  ├─ Calculate scaling parameters                                │
│  └─ Learn category encodings                                    │
│                                                                  │
│  Module: ProductionPreprocessor.fit_transform(X_train)          │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
                  ┌─────────────┐
                  │   FITTED    │
                  │ PREPROCESSOR│ ←─────────────┐
                  └──────┬──────┘               │
                         │                      │
                         │                      │
      ┌──────────────────┼──────────────────┐   │
      │                  │                  │   │
      ▼                  ▼                  ▼   │
┌──────────┐      ┌──────────┐      ┌──────────┐
│  TRAIN   │      │   VAL    │      │   TEST   │
│          │      │          │      │          │
│ TRANSFORM│      │ TRANSFORM│      │ TRANSFORM│
│          │      │          │      │          │
└─────┬────┘      └────┬─────┘      └────┬─────┘
      │                │                  │
      │                │                  │
      ▼                ▼                  │
┌───────────────────────────┐            │
│   STEP 4: TRAIN MODELS    │            │
│                           │            │
│  - Linear Regression      │            │
│  - Random Forest          │            │
│  - Gradient Boosting      │            │
│  - SVM                    │            │
│  - etc.                   │            │
│                           │            │
│  Module: ModelTrainer     │            │
└──────────┬────────────────┘            │
           │                             │
           │                             │
           ▼                             │
┌───────────────────────────┐            │
│   TRAINED MODELS          │            │
│   (Multiple models)       │            │
└──────────┬────────────────┘            │
           │                             │
           │                             │
           ▼                             ▼
┌─────────────────────────────────────────────────────────────────┐
│         STEP 5: EVALUATE ON TEST SET                             │
│                                                                  │
│  For each model:                                                │
│  - Make predictions on test set                                 │
│  - Calculate metrics (R², RMSE, MAE)                            │
│  - Compare performance                                          │
│                                                                  │
│  Module: ModelTrainer.evaluate_all()                            │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│         STEP 6: SELECT BEST MODEL                                │
│                                                                  │
│  Based on optimization metric (e.g., R²):                       │
│  - Sort models by performance                                   │
│  - Select best performing model                                 │
│                                                                  │
│  Module: ModelTrainer.select_best_model()                       │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│         STEP 7: SAVE ARTIFACTS                                   │
│                                                                  │
│  Save to models/production/:                                    │
│  ├─ model.pkl           (trained model)                         │
│  ├─ preprocessor.pkl    (fitted preprocessor)                   │
│  ├─ config.yaml         (configuration)                         │
│  ├─ metadata.json       (model info, metrics)                   │
│  └─ results.json        (all model results)                     │
│                                                                  │
│  Module: TrainingPipeline.save_artifacts()                      │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│              PRODUCTION MODEL READY! 🎉                          │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 PRODUCTION INFERENCE FLOW

```
┌─────────────────────────────────────────────────────────────────┐
│                    NEW DATA ARRIVES                              │
│             (from API, database, file, etc.)                     │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│         STEP 1: LOAD SAVED ARTIFACTS                             │
│                                                                  │
│  Load from models/production/:                                  │
│  ├─ model.pkl           ✓                                       │
│  ├─ preprocessor.pkl    ✓                                       │
│  └─ metadata.json       ✓                                       │
│                                                                  │
│  Module: InferencePipeline.__init__()                           │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│         STEP 2: VALIDATE INPUT                                   │
│                                                                  │
│  Check:                                                         │
│  ✓ Required features present                                    │
│  ✓ Correct data types                                           │
│  ✓ Value ranges reasonable                                      │
│                                                                  │
│  Module: InferencePipeline._validate_input()                    │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│         STEP 3: PREPROCESS NEW DATA                              │
│                                                                  │
│  Use SAVED preprocessor (fitted on training data):              │
│  - Apply SAME transformations                                   │
│  - Use SAME scaling parameters                                  │
│  - Use SAME encodings                                           │
│  - Handle unknown categories                                    │
│                                                                  │
│  Module: preprocessor.transform(new_data)                       │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│         STEP 4: MAKE PREDICTION                                  │
│                                                                  │
│  Use SAVED model to predict:                                    │
│  - Preprocessed data → Model → Prediction                       │
│                                                                  │
│  Module: model.predict(preprocessed_data)                       │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                 RETURN PREDICTION                                │
│                                                                  │
│  Output:                                                        │
│  - Prediction value                                             │
│  - Confidence interval (if available)                           │
│  - Feature importance (if requested)                            │
└─────────────────────────────────────────────────────────────────┘
```

## 🎯 KEY PRINCIPLES

### 1. DATA LEAKAGE PREVENTION

```
❌ WRONG:
Raw Data → Preprocess (uses all data!) → Split → Train
                ↑
            LEAKAGE!

✅ CORRECT:
Raw Data → Clean → Split → Preprocess (uses train only) → Train
                     ↑
                 NO LEAKAGE!
```

### 2. FIT vs TRANSFORM

```
TRAINING:
  preprocessor.fit_transform(train)     ← Learn + Transform
  preprocessor.transform(val)           ← Transform only
  preprocessor.transform(test)          ← Transform only

PRODUCTION:
  preprocessor.transform(new_data)      ← Transform only
```

### 3. ARTIFACT CONSISTENCY

```
models/production/
  ├─ model.pkl          ← Trained model
  ├─ preprocessor.pkl   ← SAME preprocessing used in training
  ├─ config.yaml        ← Exact configuration used
  └─ metadata.json      ← Model info for tracking
  
All must be from the SAME training run!
```

## 📈 SCALING TO PRODUCTION

### Development → Staging → Production

```
DEVELOPMENT:
  - Experiment with different models
  - Try different preprocessing
  - Use development data

STAGING:
  - Test with staging data
  - Validate performance
  - Test deployment pipeline

PRODUCTION:
  - Deploy best model
  - Monitor performance
  - Log predictions
  - A/B testing
```

### Continuous Improvement Loop

```
Production Model
      ↓
Collect New Data
      ↓
Retrain Model
      ↓
Evaluate Performance
      ↓
A/B Test
      ↓
Deploy if Better
      ↓
(loop back)
```
